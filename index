<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AI Assistant Widget</title>

  <style>
    :root {
      --accent-color: #14b8a6;
      --bg-color: #0f172a;
      --text-color: #f8fafc;
    }

    body {
      font-family: "Inter", sans-serif;
      margin: 0;
      padding: 0;
      background: var(--bg-color);
      color: var(--text-color);
    }

    /* Floating Talk with AI Button */
    #widget-button {
      position: fixed;
      bottom: 24px;
      right: 24px;
      background: var(--accent-color);
      color: #fff;
      font-weight: 600;
      border: none;
      border-radius: 50px;
      padding: 14px 24px;
      font-size: 15px;
      cursor: pointer;
      box-shadow: 0 8px 16px rgba(20, 184, 166, 0.4);
      transition: all 0.3s ease;
      z-index: 9999;
    }

    #widget-button:hover {
      transform: scale(1.05);
      box-shadow: 0 10px 24px rgba(20, 184, 166, 0.6);
    }

    /* Modal Box */
    .modal {
      display: none;
      position: fixed;
      bottom: 100px;
      right: 24px;
      width: 280px;
      background: rgba(15, 23, 42, 0.95);
      border: 1px solid rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(12px);
      border-radius: 20px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
      z-index: 10000;
      padding: 20px;
      text-align: center;
      animation: fadeIn 0.25s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .modal h3 {
      font-size: 17px;
      font-weight: 600;
      margin-bottom: 16px;
      color: #e2e8f0;
    }

    .modal-button {
      background: linear-gradient(135deg, var(--accent-color), #0d9488);
      color: #ffffff;
      border: none;
      border-radius: 12px;
      padding: 10px 18px;
      margin: 8px 0;
      width: 85%;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 10px rgba(20, 184, 166, 0.4);
    }

    .modal-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 14px rgba(20, 184, 166, 0.6);
    }

    .close-btn {
      background: none;
      border: none;
      color: #94a3b8;
      font-size: 14px;
      margin-top: 10px;
      cursor: pointer;
      transition: color 0.2s ease;
    }

    .close-btn:hover {
      color: #f1f5f9;
    }
  </style>
</head>
<body>

  <!-- Floating Button -->
  <button id="widget-button">💬 Talk with AI</button>

  <!-- Mode Selector Modal -->
  <div id="mode-select-modal" class="modal">
    <h3>How would you like to talk?</h3>
    <button class="modal-button" onclick="selectMode('chat')">💭 Chat Mode</button>
    <button class="modal-button" onclick="selectMode('voice')">🎙️ Voice Mode</button>
    <button class="close-btn" onclick="closeModal()">Cancel</button>
  </div>

  <!-- Widget Container -->
  <div id="widget-container"></div>

  <script>
    const widgetButton = document.getElementById('widget-button');
    const modal = document.getElementById('mode-select-modal');
    const widgetContainer = document.getElementById('widget-container');

    widgetButton.addEventListener('click', () => {
      modal.style.display = 'block';
      widgetButton.style.display = 'none';
    });

    function closeModal() {
      modal.style.display = 'none';
      widgetButton.style.display = 'block';
    }

    function selectMode(mode) {
      modal.style.display = 'none';

      const widget = document.createElement('vapi-widget');
      widget.setAttribute('public-key', 'aa01c84f-db9c-470b-811a-89bf7b9e026c');
      widget.setAttribute('assistant-id', 'e3719d19-8df5-4fc9-8a17-49d502803365');
      widget.setAttribute('mode', mode);
      widget.setAttribute('theme', 'dark');
      widget.setAttribute('base-color', '#000000');
      widget.setAttribute('accent-color', '#14B8A6');
      widget.setAttribute('button-base-color', '#000000');
      widget.setAttribute('button-accent-color', '#ffffff');
      widget.setAttribute('radius', 'large');
      widget.setAttribute('size', 'compact');
      widget.setAttribute('position', 'bottom-right');
      widget.setAttribute('main-label', 'Talk with AI');
      widget.setAttribute('start-button-text', 'Start');
      widget.setAttribute('end-button-text', 'End Call');
      widget.setAttribute('empty-chat-message', 'Hey, how can I help you today?');
      widget.setAttribute('show-transcript', 'true');
      widget.setAttribute('require-consent', 'true');
      widget.setAttribute('terms-content', 'By clicking "Agree," you consent to the recording, storage, and sharing of your interactions as described in our Terms of Service.');
      widget.setAttribute('local-storage-key', 'vapi_widget_consent');

      widgetContainer.innerHTML = '';
      widgetContainer.appendChild(widget);

      const script = document.createElement('script');
      script.src = 'https://unpkg.com/@vapi-ai/client-sdk-react/dist/embed/widget.umd.js';
      script.type = 'text/javascript';
      document.body.appendChild(script);
    }
  </script>
</body>
</html>
